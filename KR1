#include <iostream>

int main()
{
    int i, N;
    std::cin >> N;
    i = 1;
    if (N >= 0)
    {
         while (i <= N)
        {
            i = i*2;
        }
        if ((N % (i/2)) == 0)
            std::cout << "1";
    }

    return 0;
}
