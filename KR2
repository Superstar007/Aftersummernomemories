#include <iostream>

int main()
{
    int a[1000], i , n;
    std::cin >> n;
    for (i = 0; i < n; i++)
        std::cin >> a[i];
    for (i = 0; i < n; i++)
        if ((a[i] % 7 == 0) || (a[i] % 7 == 0) || (a[i] % 9 == 0) || (a[i]/1000 == 7) || (a[i]/1000 == 1) ||(a[i]/1000 == 7) || (a[i]/1000 == 5) || (a[i]/1000 == 4) || (a[i]/1000 == 8) || (a[i]/1000 == 9))
            std::cout << a[i] << " ";
    return 0;
}
